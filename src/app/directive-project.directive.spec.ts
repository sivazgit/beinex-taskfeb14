import { DirectiveProjectDirective } from './directive-project.directive';

describe('DirectiveProjectDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveProjectDirective();
    expect(directive).toBeTruthy();
  });
});
